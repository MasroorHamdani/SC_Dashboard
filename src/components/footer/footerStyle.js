const styles = theme => ({
    appBar: {
        bottom: 0,
        top: 'auto',
        zIndex: theme.zIndex.drawer + 1,
      },
      title: {
        flexGrow: 1,
        textAlign: 'center',
      }
});

export default styles;